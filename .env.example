# XRPL Monitor Configuration

# ============================================================================
# CONNECTION CONFIGURATION
# ============================================================================

# rippled Connection
# Works for both native rippled and Docker rippled (monitor uses host networking)
RIPPLED_WS_URL=ws://localhost:6006
RIPPLED_HTTP_URL=http://localhost:5005

# VictoriaMetrics Connection
# For Docker monitor with host networking: Use localhost
# For native Python: Use localhost
VICTORIA_METRICS_URL=http://localhost:8428

# ============================================================================
# PORT CONFIGURATION
# ============================================================================
# Customize these if the defaults conflict with other services on your system.
# After changing ports, run: ./manage.sh update

# Grafana Web UI Port (default: 3000)
# Access dashboard at http://your-server:GRAFANA_PORT
#GRAFANA_PORT=3000

# VictoriaMetrics Port (default: 8428)
# Time-series database for metrics storage
#VICTORIA_METRICS_PORT=8428

# Collector Health Check Port (default: 8090)
# Internal health endpoint for the metrics collector
#COLLECTOR_PORT=8090

# vmagent Port (default: 8427)
# Metrics scraping agent
#VMAGENT_PORT=8427

# Node Exporter Port (default: 9100)
# System metrics (CPU, memory, disk, network)
#NODE_EXPORTER_PORT=9100

# Uptime Exporter Port (default: 9101)
# rippled uptime metrics
#UPTIME_EXPORTER_PORT=9101

# State Exporter Port (default: 9102)
# Real-time rippled state metrics
#STATE_EXPORTER_PORT=9102

# ============================================================================
# GRAFANA CONFIGURATION
# ============================================================================

# Grafana Admin Credentials (default: admin/admin)
# IMPORTANT: Change the password after first login!
#GRAFANA_ADMIN_USER=admin
#GRAFANA_ADMIN_PASSWORD=admin

# ============================================================================
# VALIDATOR CONFIGURATION
# ============================================================================

# Validator Public Key (starts with 'nH')
# Set this to your validator's public key for validation tracking
# Example: VALIDATOR_PUBLIC_KEY=nHB6bPcp9jk8QbUZiGXoonERK9rcDvjZUJAoFsGVCs2ZgUdCtnSV
VALIDATOR_PUBLIC_KEY=

# ============================================================================
# RIPPLED DATA PATHS
# ============================================================================

# rippled Data Paths (optional, auto-discovery enabled)
# Set these if rippled uses custom paths. Monitor will auto-discover standard locations:
#   - /var/lib/rippled/db/nudb (current standard)
#   - /var/lib/rippled/nudb (legacy)
#
# For Docker host mounts, set to host path (e.g., /home/username/rippled/data)
# Example: RIPPLED_DATA_PATH=/home/username/rippled/data
#RIPPLED_DATA_PATH=
#RIPPLED_NUDB_PATH=

# ============================================================================
# DOCKER CONFIGURATION
# ============================================================================

# Docker Container Name (optional)
# Set this if rippled runs in Docker to enable peer metrics via docker exec fallback
# For native installs, leave this commented out or remove it
# Example: RIPPLED_DOCKER_CONTAINER=rippledvalidator
#RIPPLED_DOCKER_CONTAINER=

# Docker Group ID (Docker deployment only)
# Required for monitor container to access Docker socket
# Find your docker GID with: getent group docker | cut -d: -f3
# Usually 999, but may vary by system
#DOCKER_GID=999

# ============================================================================
# EMAIL ALERTS (SMTP Configuration)
# ============================================================================
# Configure these settings to receive email notifications for alerts.
# Works with any SMTP provider - examples below.
#
# IMPORTANT: These settings are preserved during updates (git pull + ./manage.sh)
#
# Common SMTP servers:
#   Gmail:           smtp.gmail.com:587         (requires App Password)
#   Outlook/O365:    smtp.office365.com:587
#   Yahoo:           smtp.mail.yahoo.com:587
#   AWS SES:         email-smtp.us-east-1.amazonaws.com:587
#   SendGrid:        smtp.sendgrid.net:587
#   Mailgun:         smtp.mailgun.org:587
#
# Gmail App Password setup:
#   1. Go to Google Account → Security → 2-Step Verification (enable if needed)
#   2. Go to App Passwords → Generate a new app password
#   3. Use that 16-character password below

#GF_SMTP_ENABLED=true
#GF_SMTP_HOST=smtp.gmail.com:587
#GF_SMTP_USER=your-email@example.com
#GF_SMTP_PASSWORD=your-password-or-app-password
#GF_SMTP_FROM_ADDRESS=your-email@example.com

# ============================================================================
# LOGGING
# ============================================================================

# Log Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
